BELM PORTAL V212 - BILLING CUSTOMER LOOKUP FIX

Fixed:
- Billing Manager customer dropdown no longer depends on Customers Manager permission.
- Added billing-scoped read-only customer + machine lookup.
- Spare inventory picker no longer depends on Spare Parts Manager permission.
- Added billing-scoped read-only spare lookup (no purchase price exposed).
- Billing core data can still load even if a lookup fails; the error is shown clearly.

No database migration required.
