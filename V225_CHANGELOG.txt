BELM PORTAL V225 - CUSTOMER DASHBOARD QUICK ROW LAYOUT

- Petty Cash and Breakdown Process cards on the customer dashboard now
  sit side by side in one row (like Machine Attention / Service Due)
  instead of two separate full-width bars stacked on top of each other.
- Breakdown Process is more compact in the row (smaller icon, no arrow
  glyph) so it reads like a normal button rather than a wide banner.
- Below 640px width (small phones) it automatically falls back to the
  original full-width stacked cards so nothing gets cramped.
- Browser/PWA cache bumped to V225.
- No database migration required.
