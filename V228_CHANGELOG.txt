BELM PORTAL V228 - MAINTENANCE PROCESS MOVED INTO MORE TOOLS

- "Breakdown Process" renamed to "Maintenance Process" (English and
  Kiswahili dashboard translation).
- It is no longer a separate card/row on the Customer Dashboard. It is
  now a button inside the MORE TOOLS card, positioned between +USER
  and Request BELM Support (order: +USER, Maintenance Process, Request
  BELM Support, Management Email). Same destination and permission
  rule as before (hidden if the customer's Role Manager permissions
  don't include "workflow").
- Petty Cash remains inside the single unified overview card (Status +
  Petty Cash), unchanged from V227.
- Removed the now-unused "Breakdown Process rail card" CSS.
- Browser/PWA cache bumped to V228.
- No database migration required.
