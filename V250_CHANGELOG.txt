BELM PORTAL V250 - MORE TOOLS CARD: CLEANER + STICKY

- Removed the "CUSTOMER MAINTENANCE TEAM / BELM SERVICE PROVIDER
  ACTIVE" operating-mode message box from the MORE TOOLS card - one
  less thing crowding the card, buttons now start right under the
  heading.
- Reduced button size slightly (min-height 40px -> 32px, padding
  6px 4px -> 5px 4px, font-size 9.5px -> 8.5px) so the card reads
  tighter and more polished, matching the request to shrink things
  down a bit.
- MORE TOOLS now sticks in place while scrolling (position: sticky),
  same behavior as the Status/Petty Cash overview card above it, so it
  stays visible and accessible instead of scrolling out of view. Falls
  back to normal (non-sticky) placement on small phones (<=480px)
  where sticky elements tend to fight with the on-screen keyboard.
- Browser/PWA cache bumped to V250.
- No database migration required.
