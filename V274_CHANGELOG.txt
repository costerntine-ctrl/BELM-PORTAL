BELM PORTAL V274 - "CHECKUP REPORT" BUTTON (BELM SIDE) + SMALLER CHECK UP BUTTON

- The "Check Up → Fill checklist for this machine" button in the
  Message Customer dialog is now shorter ("Check Up → Fill checklist")
  and sits side by side with a new button instead of taking the full
  width alone.
- New button: "Checkup Report". Opens the same Reports dialog used
  elsewhere, now showing TWO sections for the selected machine:
  Checklist Reports (as before) and, underneath, Job Card / Daily
  Report - every Digital Job Card for that machine, with a Download
  link for each. Both buttons stay disabled until a specific machine
  is chosen in "Related machine", same as before.

BACKEND
- New: GET /api/breakdown-workflow?action=machine-job-cards&machineId=
  - staff-authenticated (Technician or page-access-checked staff),
  read-only, returns a machine's Job Card history for this view. Fixed
  a variable-naming slip while adding it (used require_auth()'s own
  return value directly instead of an undefined $user).

- Browser/PWA cache bumped to V274.
- No database migration required.
