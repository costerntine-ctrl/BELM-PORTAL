BELM PORTAL V277 - CONSOLIDATED: CHECKUP REPORT + SEND MESSAGE

1. REMOVED "Checkup Report" from Message Customer dialog
   - It duplicated the existing "Report" button on the machine card
     (View Machines), which already opens the exact same combined
     Checklist Reports + Job Card/Daily Report view (V274). No feature
     was lost - it's still one click away on the machine card itself,
     just no longer duplicated here too.
   - "Check Up → Fill checklist" stays in Message Customer as before
     (not part of this change).

2. ADDED "✉ Send Message to Customer" to Edit Machine
   - New button next to Cancel/Save machine. Closes Edit Machine and
     opens the same Message Customer dialog, with this specific
     machine already selected in "Related machine" - so you can flag
     something about the machine you're editing without leaving to
     find the customer card first.
   - This reuses the existing Message Customer dialog/logic directly -
     there is still only one messaging implementation in the codebase,
     just reachable from a second, more convenient entry point.

- Browser/PWA cache bumped to V277.
- No database migration required.
