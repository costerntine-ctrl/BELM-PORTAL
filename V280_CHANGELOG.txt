BELM PORTAL V280 - FULL PROJECT AUDIT (post V270-V279 changes)

Requested full sweep given how much was recently edited (V270-V279:
customers-manager card reorder, dialog back-button, message/checkup
consolidation, sidebar search, etc.). Results:

CHECKED - all clean
- JavaScript: all 36 frontend .js files pass `node --check`.
- CSS: all 27 files brace-balanced.
- PHP: all 41 backend files brace/paren-balanced.
- HTML: dialog/section/form/article tags balanced on every page; no
  duplicate element IDs anywhere.
- Cross-referenced every getElementById() call in portal-tools.js,
  customers-manager/manager.js, and breakdown-workflow/workflow.js
  against every id="..." actually defined (in HTML or via JS template
  strings) - zero dangling references found. The one apparent "miss"
  (evidencePhotoLightbox) is an intentional get-or-create pattern, not
  a bug.
- Verified no leftover console.log/debugger statements in the files
  touched this round.
- Version-string consistency: admin-sidebar.js/.css point to the same
  single latest version (v279) on all 17 admin pages + Breakdown
  Workflow; customers-manager, portal-tools.js/belm-theme.css are each
  internally consistent at their own latest version.
- Full regression suite (v211-v223): all real checks passing, 0
  failures.

FIXED - one minor housekeeping item found during this sweep
- backend/api/customer_portal.php: a code comment I wrote in V273
  used mathematical half-open-interval notation "[from, to)", whose
  literal ")" character made an automated parenthesis-balance check
  report a false mismatch. PHP itself was never affected (comments are
  ignored by the parser regardless), but reworded it anyway so future
  automated audits of this file read cleanly too. No behavior change.

- Browser/PWA cache bumped to V280.
- No database migration required.
