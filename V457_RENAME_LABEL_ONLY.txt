V457 - Sidebar label rename only (no functional change)

admin-sidebar.js: the "customers" entry's label changed from
"Customers & Machines" to "TECHNICAL DEP" (short code kept as "CM",
href/paths/key unchanged - still /customers-manager/).

Nothing else touched: the page's content stays exactly as it is now
(Customer Overview - hero, metrics, Machine Type <-> Checklist Template
Sync, search/filter, customer cards). This is a label-only change, as
requested.
