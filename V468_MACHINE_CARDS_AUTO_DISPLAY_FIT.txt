V468 - MACHINE CARDS AUTO DISPLAY FIT

Scope:
BELM Admin > Customer Overview > View Customer Machine.

Changes:
- Fleet dialog now expands responsively up to 1560px while staying within 96vw.
- Machine card grid uses automatic responsive columns.
- Two machine cards appear side-by-side only when enough width is available.
- Otherwise one card remains centered and keeps the approved machine-card design.
- Machine cards use natural content height (no fixed card height).
- Operational and admin action buttons automatically reflow as card width narrows.
- Mobile cards use two action columns, then one column on very narrow phones.
- No backend, Job Card, machine data, permissions or service logic changed.
