BELM PORTAL V495 - BELM MAIN HOME

- PORTAL-BELM WM is now the BELM Technical Department Main Home.
- Non-Technician BELM staff unified login lands on /belm-workshop/.
- Existing stored BELM admin sessions resume to /belm-workshop/.
- Sidebar brand and nested Back links return to BELM Main Home.
- Standalone admin page home/back navigation returns to Main Home instead of the old Overview landing page.
- Overview is NOT deleted: General Analysis on Main Home still opens /overview-manager/ inside the WM workspace.
- Delivery Notes remains on the Main Home action grid and still opens the existing Delivery Note workflow.
- Workspace Back now closes the module and returns to the Main Home card.
- Service-worker cache bumped to v495-main-home.
- BELM app favicon added to Main Home to remove the generic white browser-tab square.
