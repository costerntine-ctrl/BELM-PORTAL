BELM PORTAL V231 - GENERAL BUG SWEEP

Checked, no code changes needed beyond one stale test fix:
- JavaScript syntax: all frontend .js files pass `node --check`.
- CSS: all frontend .css files have balanced braces (no unclosed rules).
- PHP: all backend .php files have balanced braces/parentheses
  (heuristic check; a real php -l lint requires a PHP CLI, which isn't
  available in this environment - if you can, run `php -l` on each
  file after deploying as a final check).
- HTML: no duplicate element IDs found in any frontend page.
- Regression suite: re-ran v211/v215/v217/v218/v219/v220/v221/v222/v223.
  All real checks pass. The "cache bumped" failures in v220/v221/v222/
  v223 are stale tests checking for older exact version strings that
  were legitimately superseded by V224-V230 - not functional bugs.

FIXED
- tests/v215-regression.cjs: updated one stale assertion
  ("Dashboard Petty Cash is in sticky rail stack") that was checking
  for the pre-V227 DOM structure (`overviewStack.appendChild(pettyCashCard)`).
  Since V227, Petty Cash is nested inside the single unified overview
  card (`overviewCard.appendChild(pettyCashCard)`) - functionally the
  same sticky rail placement, just organized under one shared card.
  The test now checks the current structure. This is a test-only fix;
  no application behavior changed.

NOT RUN
- tests/ui-smoke.cjs requires a jsdom-based browser harness
  (BELM_JS_TOOLS env var) that isn't installed in this build
  environment - same limitation noted since the V211 bug audit.
  Recommend a manual/live smoke test after deploying, especially on
  one phone and one PC.

No database migration required. No cache version bump - no runtime
files changed in this build, only project scripts (tests folder).
