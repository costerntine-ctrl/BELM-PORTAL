BELM PORTAL V243 - SEND TO BELM, DAILY REPORT, SIGNABLE JOB CARD PDF

Three-part request, all delivered:

1. "SEND TO BELM" OPTION ON THE JOB CARD DIALOG
- The "Generate Digital Job Card" dialog (customer side) now has a
  "→ Send to BELM instead (Request Support)" button under the
  Technician picker, with a short explainer line.
- Clicking it takes the customer straight to the Request BELM Support
  (Service Request) page, machine pre-selected and the breakdown's
  description pre-filled into the request note - this is the correct,
  already-supported channel for BELM to see and act on a self-service
  customer's case (a direct Job Card assignment to a BELM technician
  isn't visible to BELM unless it comes through an official Support
  Request - that rule was already in place and is unchanged here).

2. DAILY REPORT RECORD ON COMPLETION
- Verified (no code change needed): completing a Digital Job Card
  already flows straight into the Workshop Department Report the
  moment `completed_at` falls within that day - open Breakdown
  Workflow -> Overview -> Daily to see it, per-technician and overall
  (Jobs completed, completion rate, first-time fix rate, avg
  resolution time). This was already working correctly.

3. SIGNABLE HARDCOPY JOB CARD PDF
- The Job Card PDF (Download Job Card PDF) now ends with two signature
  lines - Technician Signature and Customer / Supervisor Signature,
  each with a Date field - and a note: "Print, sign, and keep this
  copy for office records."

- Browser/PWA cache bumped to V243.
- No database migration required.

ALSO FIXED (found while wiring this up)
- tests/v223-regression.cjs still checked for the old
  "workflow.js?v=223-tech-sync" exact string, which this update
  legitimately moved past (now v=243). Same brittle-test issue fixed
  project-wide in V242 - updated this one the same way (checks the
  cache-busted version number is >= 223 instead of an exact string).
  Full suite: 15/15 passing again.
