BELM PORTAL V271 - MESSAGES FROM BELM: RED ALERT + BLINKING

- Every message in the "Messages from BELM" panel (Customer Dashboard)
  now shows with a red border that blinks continuously until the
  customer dismisses it with the existing "OK" button (V229) - a new
  message can no longer blend quietly into the page.
- Respects the "reduce motion" accessibility setting (steady red
  border instead of blinking, if the browser/OS requests it).
- The "UPDATE" button in MORE TOOLS (V254) now also blinks red for any
  undismissed BELM message, not just a RED machine - a new message is
  just as urgent a signal. It stops blinking once every message has
  been dismissed (or the machine issue clears), same as before.

- Browser/PWA cache bumped to V271.
- No database migration required.
