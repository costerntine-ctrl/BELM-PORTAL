BELM PORTAL V282 - CUSTOMER CARD BLINK + COMMUNICATION HISTORY DARK FIX

1. CUSTOMER CARD NOW BLINKS BY MACHINE STATUS
- Each customer card's border now blinks red if any of that
  customer's machines is RED/CRITICAL, or blinks yellow (softer,
  slower pulse) if the worst is YELLOW/ATTENTION - no blink at all if
  every machine is GREEN. You can now spot which customers need
  attention while scrolling the list, without opening View Machines
  for each one.
- Respects "reduce motion" (steady colored border instead of blinking,
  if the browser/OS requests it).

2. FOUND & FIXED - Communication history had no Dark Mode styling
- The "Communication history" box had zero Dark Mode rules at all, so
  it stayed on its light blue/white background and border even with
  everything else on the page dark - that stuck-out brightness is what
  read as a "glow" around it.
- Given a proper black/dark tint in Dark Mode: dark navy background,
  dark border, light text - matching the rest of the dark theme
  instead of standing out.

- Browser/PWA cache bumped to V282.
- No database migration required.
