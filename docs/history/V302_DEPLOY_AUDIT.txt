BELM Portal V302 - Deploy Readiness Audit
Date: 2026-08-18

CURRENT DEPLOY GATE
PASS - 37/37 JavaScript syntax checks
PASS - 47/47 PHP syntax checks
PASS - docker/start-render.sh shell syntax
PASS - render.yaml YAML parse
PASS - 316 local HTML asset references (0 missing)
PASS - 72 static PHP include references (0 missing)
PASS - V301 Customer Job Card + Billing Sync regression: 23/23
PASS - V302 Deploy Readiness regression: 22/22
PASS - Source ZIP integrity

DEPLOY BLOCKERS FOUND AND FIXED
1. Docker Apache friendly /app route did not accept @ in TECH@BELM/name@BELM. Fixed.
2. Fresh/reset bootstrap contained a known Admin password. Removed usable source default; secure bootstrap now uses INITIAL_ADMIN_PASSWORD.
3. Full database reset restored public/default Admin credentials. Fixed: current Admin identity/password and edit/delete PINs are preserved.
4. repair_admin.sql could restore known credentials and used an unsafe conflict target. Fixed.
5. /api/health used obsolete Admin email and did not check V301 billing columns. Fixed.
6. /api/health now returns 503 when the required schema is incomplete so Render does not mark a broken migration healthy.

HISTORICAL TEST NOTE
The archive retains regression scripts from older releases V285-V299. Several intentionally assert old cache-bust labels or workflows that later versions replaced (for example the V295/V297 spare workflow changed in V298). They are historical tests, not the V302 deploy gate. ui-smoke.cjs additionally requires an external BELM_JS_TOOLS/jsdom harness that is not bundled with the deploy archive. Current release gates V301 and V302 pass completely.

RENDER SECRET
If this is a fresh deployment, or the seeded Admin still uses the legacy bootstrap hash, set INITIAL_ADMIN_PASSWORD to a strong value of at least 12 characters before starting the service. Existing Admin passwords that were already changed are preserved.
