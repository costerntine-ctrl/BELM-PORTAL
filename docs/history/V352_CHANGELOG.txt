BELM PORTAL V352 - PUBLIC URL PORT GUARD

Problem fixed
- Public login links could inherit Render/Apache's internal listener port and become:
  https://portal.belmgeneraltech.co.tz:10000/login
- Port 10000 is for the container listener only. It must never be published to phones,
  emails, QR links, customer credentials or browser redirects.

Changes
1. portal_base_url() now prefers the configured PORTAL_URL before request host headers.
2. HTTPS :10000 is stripped defensively from configured/fallback public URLs.
3. public_app_base_url() also strips HTTPS :10000 if accidentally configured.
4. Render continues listening on $PORT internally; Docker/start-render networking is unchanged.
5. No schema.sql change and no business-data migration.
6. V350 data-preservation guard remains active.

Canonical portal login
https://portal.belmgeneraltech.co.tz/login
