(()=>{const token=localStorage.getItem('belm_admin_token');if(!token){location.replace('/login');return}let user=null;try{user=JSON.parse(localStorage.getItem('belm_admin_user')||'null')}catch(_){}const role=String(user?.role||'').toLowerCase();if(role!=='super admin'&&user?.allowedPages!==null){location.replace('/belm-workshop/');return}let all=[];const box=document.getElementById('customers'),alertBox=document.getElementById('alert'),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(path,opt={}){const r=await fetch('/api'+path,{...opt,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`,...(opt.headers||{})},cache:'no-store'}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||`Request failed (${r.status})`);return d}function render(){const q=document.getElementById('search').value.toLowerCase().trim(),rows=all.filter(x=>!q||`${x.name} ${x.email}`.toLowerCase().includes(q));box.innerHTML=rows.length?rows.map(c=>{const f=c.coordinatorFeatures||{},connected=!c.selfServiceEnabled;return `<article class="customer" data-id="${esc(c.id)}"><div><h3>${esc(c.name)}</h3><small>${esc(c.email||'')} · ${esc(c.phone||'')}</small></div><div><span class="mode ${connected?'connected':'independent'}">${connected?'CONNECTED TO BELM':'INDEPENDENT CUSTOMER'}</span></div><div class="switches"><label class="toggle"><input type="checkbox" data-feature="belm" ${connected?'checked':''}> BELM Service</label><label class="toggle"><input type="checkbox" data-feature="invoiceSystem" ${f.invoiceSystem?'checked':''}> Invoice System</label><label class="toggle"><input type="checkbox" data-feature="proformaSystem" ${f.proformaSystem?'checked':''}> Proforma System</label></div></article>`}).join(''):'<div class="empty">No matching customer.</div>';wire()}async function confirmEdit(message){if(!window.belmConfirmEdit)return null;return await window.belmConfirmEdit({title:'Coordinator change',message})}function wire(){box.querySelectorAll('input[data-feature]').forEach(inp=>inp.addEventListener('change',async()=>{const card=inp.closest('.customer'),id=card.dataset.id,c=all.find(x=>x.id===id),feature=inp.dataset.feature,desired=inp.checked;inp.disabled=true;try{const conf=await confirmEdit(`Confirm ${desired?'enable':'disable'} ${feature==='belm'?'BELM Service':feature} for ${c.name}. No customer data will be deleted.`);if(!conf){inp.checked=!desired;return}if(feature==='belm'){await api(`/customers/${id}/machinery-admin`,{method:'PUT',body:JSON.stringify({serviceProviderEnabled:desired,editPin:conf.editPin})});c.selfServiceEnabled=!desired}else{const f={...(c.coordinatorFeatures||{}),[feature]:desired};await api(`/customers/${id}/coordinator-features`,{method:'PUT',body:JSON.stringify({...f,editPin:conf.editPin})});c.coordinatorFeatures=f}render()}catch(e){inp.checked=!desired;alertBox.textContent=e.message;alertBox.hidden=false}finally{inp.disabled=false}}))}async function load(){alertBox.hidden=true;try{all=await api('/customers/cwm-overview');render()}catch(e){box.innerHTML=`<div class="empty">${esc(e.message)}</div>`}}document.getElementById('search').oninput=render;document.getElementById('refresh').onclick=load;load()})();
