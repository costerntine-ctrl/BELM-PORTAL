const fs=require('fs');
const path=require('path');
const root=path.resolve(__dirname,'..');
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
const schema=read('backend/schema.sql');
const helpers=read('backend/config/helpers.php');
const workflowApi=read('backend/api/breakdown_workflow.php');
const workflowJs=read('frontend/breakdown-workflow/workflow.js');
const workflowHtml=read('frontend/breakdown-workflow/index.html');
const customerApi=read('backend/api/customer_portal.php');
const procHtml=read('frontend/customer-procurement/index.html');
const procJs=read('frontend/customer-machine-expenses/expenses.js');
const billingApi=read('backend/api/billing.php');
const billingJs=read('frontend/billing-manager/manager.js');
const billingCss=read('frontend/billing-manager/manager.css');
const proformaApi=read('backend/api/proforma_invoices.php');
const receiptsApi=read('backend/api/receipts.php');
const invoicePdf=read('backend/api/invoice_pdf_helper.php');
const sw=read('frontend/belm-sw.js');
const portalIndex=read('frontend/index.html');
let pass=0, fail=0;
function test(name,cond){if(cond){console.log('PASS',name);pass++;}else{console.log('FAIL',name);fail++;}}

test('job card stores issuer and customer signoff',schema.includes('issued_by_name VARCHAR(255)')&&schema.includes('signed_copy_data TEXT')&&schema.includes("billing_status VARCHAR(40) NOT NULL DEFAULT 'NOT_READY'"));
test('proforma and invoice link to source Job Card',schema.includes('ALTER TABLE proforma_invoices ADD COLUMN IF NOT EXISTS source_job_card_id')&&schema.includes('ALTER TABLE invoices ADD COLUMN IF NOT EXISTS source_job_card_id'));
test('official service request auto-creates one Job Card',helpers.includes('function belm_ensure_service_request_job_card')&&helpers.includes("source_type='SERVICE_REQUEST'")&&helpers.includes("issued_by_type"));
test('service assignment syncs technician into same Job Card',helpers.includes('assigned_to_id')&&helpers.includes('UPDATE digital_job_cards SET technician_id=?,technician_name=?')&&helpers.includes('assigned_to_name'));
test('Job Card PDF prints Issued By and customer signoff',workflowApi.includes("['Issued By'")&&workflowApi.includes("['Customer Sign-Off'")&&workflowApi.includes('Customer / Supervisor Signature'));
test('signed Job Card upload endpoint exists',workflowApi.includes("signed-job-card'")&&workflowApi.includes('READY_FOR_PROCUREMENT')&&workflowApi.includes('signed_copy_data'));
test('signed Job Card file endpoint exists',workflowApi.includes('signed-job-card-file'));
test('workflow UI uploads signed Job Card',workflowJs.includes('Upload Customer-Signed Job Card')&&workflowJs.includes('Replace Signed Copy'));
test('workflow UI prepares Proforma and Invoice',workflowJs.includes('Prepare Proforma')&&workflowJs.includes('Prepare Invoice')&&workflowJs.includes('belm_prefill_invoice')&&workflowJs.includes('belm_prefill_proforma'));
test('customer Procurement has service billing panel',procHtml.includes('BELM Service Job Billing')&&procHtml.includes('Payment'));
test('customer Procurement exposes Proforma and Invoice downloads',procJs.includes('Proforma')&&procJs.includes('Invoice')&&procJs.includes('/api/customer-portal/invoices/')&&procJs.includes('/api/customer-portal/proformas/'));
test('customer Procurement colors paid/outstanding',procJs.includes('PAID')&&procJs.includes('OUTSTANDING')&&read('frontend/customer-machine-expenses/expenses.css').includes('.sync-status.outstanding')&&read('frontend/customer-machine-expenses/expenses.css').includes('.sync-status.paid'));
test('customer API supplies service Job Card billing rows',customerApi.includes('customer_service_job_billing_rows')&&customerApi.includes("'serviceJobBilling'"));
test('customer invoice download endpoint exists',customerApi.includes("$sub === 'invoices'")&&customerApi.includes('belm_output_invoice_document_pdf'));
test('reusable invoice PDF helper exists',invoicePdf.includes('function belm_output_invoice_document_pdf'));
test('BELM invoice links source Job Card and syncs status',billingApi.includes('sourceJobCardId')&&billingApi.includes('function belm_sync_invoice_job_card')&&helpers.includes('function belm_recompute_job_billing_status')&&helpers.includes('INVOICE_OUTSTANDING'));
test('BELM Billing shows yellow outstanding and green paid',billingJs.includes('invoice-sync-badge')&&billingCss.includes('.invoice-sync-badge.outstanding')&&billingCss.includes('.invoice-sync-badge.paid'));
test('Proforma syncs source Job Card state',proformaApi.includes('sourceJobCardId')&&proformaApi.includes('belm_recompute_job_billing_status')&&helpers.includes('PROFORMA_READY')&&helpers.includes('PROFORMA_SENT'));
test('receipt payment syncs linked Job Card',receiptsApi.includes('source_job_card_id')&&receiptsApi.includes("$jobBillingStatus = $newStatus === 'PAID' ? 'PAID' : 'INVOICE_OUTSTANDING'"));
test('workflow assets cache-busted to V301',workflowHtml.includes('301-job-signoff'));
test('procurement assets cache-busted to V301',procHtml.includes('301-service-billing'));
test('shared theme cache-busted to V301',portalIndex.includes('301-customer-job-billing'));
test('service worker cache bumped to V301',sw.includes('belm-app-v301-customer-job-billing'));
console.log(`\n${pass}/${pass+fail} V301 checks passed`);
process.exit(fail?1:0);
