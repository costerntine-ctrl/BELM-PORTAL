const fs=require('fs');
const path=require('path');
const root=path.resolve(__dirname,'..');
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
let pass=0,fail=0;function test(name,ok){if(ok){console.log('PASS',name);pass++;}else{console.error('FAIL',name);fail++;}}
const engApi=read('backend/api/engineering.php');
const engHtml=read('frontend/engineering-manager/index.html');
const engJs=read('frontend/engineering-manager/manager.js');
const bwApi=read('backend/api/breakdown_workflow.php');
const bwJs=read('frontend/breakdown-workflow/workflow.js');
const portal=read('frontend/portal-tools.js');
const schema=read('backend/schema.sql');
const health=read('backend/index.php');
const techHtml=read('frontend/technician-job-cards/index.html');
const techJs=read('frontend/technician-job-cards/job-cards.js');
test('dispatch received/create modes remain supported by backend after V317 UI removal',engApi.includes("$mode==='existing'")&&engJs.includes('jobCardMode:mode')&&!engHtml.includes('id="dispatchPanel"'));
test('dispatch options returns received job cards and machines',engApi.includes("'receivedJobCards'")&&engApi.includes("'machines'")&&engApi.includes("source_type='SERVICE_REQUEST'"));
test('received Job Card assignment updates same card',engApi.includes('UPDATE digital_job_cards SET technician_id=?')&&engApi.includes('Assigned through received Job Card'));
test('create mode creates breakdown case and Digital Job Card',engApi.includes("$mode==='existing'")&&engApi.includes("INSERT INTO breakdown_cases")&&engApi.includes("INSERT INTO digital_job_cards"));
test('dispatch preserves temporary override protection',engApi.includes('temporaryOverride')&&engJs.includes('Temporary Override'));
test('job cards retain priority and due date',schema.includes("ADD COLUMN IF NOT EXISTS priority")&&schema.includes("ADD COLUMN IF NOT EXISTS due_date"));
test('health checks V305 job card columns',/\['digital_job_cards',\s*'priority'\]/.test(health)&&(() => { const m = /'schemaVersion' => '(\d+)-/.exec(health); return m && Number(m[1]) >= 305; })());
test('dedicated Technician Job Card workspace exists',techHtml.includes('My Job Cards')&&techHtml.includes('My Reports')&&techHtml.includes('Job Card Workspace'));
test('Technician page uses dedicated compact API',techJs.includes('/technician-jobs')&&bwApi.includes("action === 'technician-jobs'"));
test('Technician list scoped to assigned technician',bwApi.includes("$where=['j.technician_id=?']"));
test('Technician can update report inside Job Card workspace',techJs.includes('/job-report/')&&techHtml.includes('Save Job Report'));
test('Technician can request spare from Job Card',techJs.includes("api('/spare'")&&techHtml.includes('Request Spare'));
test('Technician can download reference PDF',techJs.includes('job-card-pdf')&&techJs.includes('Download Report PDF'));
test('Technician sees simplified work status, not department analysis',techJs.includes('Waiting spare / approval')&&!techHtml.includes('Workshop Department Analysis'));
test('old Technician workflow redirects to My Job Cards',bwJs.includes("location.replace(`/technician-job-cards/"));
test('Technician shortcut points to My Job Cards',portal.includes('link.href = "/technician-job-cards/"')&&portal.includes('My Job Cards'));
test('machine Technician action no longer opens full Maintenance Process',portal.includes('customerWorkflowActor() === "tech" ? `/technician-job-cards/'));
test('Engineering frontend sends Job Card mode',engJs.includes('jobCardMode:mode')&&engJs.includes('jobCardId'));
console.log(`\n${pass}/${pass+fail} V305 checks passed`);process.exit(fail?1:0);
